const notas = [6.4, 7.4, 8.1, 5.7, 1.8, 3.8, 7.0, 1.4]

for (let i in notas) {
    console.log(i, notas[i])
}

const pessoa = {
    nome: 'Ana',
    sobrenome: 'Silva',
    idade: 30,
    peso: 64
}

for (let a in pessoa){
    console.log(`${a}: ${pessoa[a]}`)
    
}

